#include "Barfight.h"
#include "Player.h"
void Barfight::applyEncounter(Player& player)
{
    if(player.getname().compare("Fighter"))
    {
        return;
    }
    else {
        //look at later
//        player.damage(m_hpEffect);
        return;
    }
}


